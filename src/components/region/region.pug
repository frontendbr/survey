- var css = require('./region-css.json');

section(class=css.host)
  h1(class=css.title) Respostas por região

  article(class=css.chart)
    canvas(class=css.canvas width='300' height='300' data-chart='region')
